import {
    //action types
} from '../actions'

const initalState ={

}

export const potluckViewReducer = (state = initalState, action) => {
    switch(action.type) {
        case INVITE_GUEST:
            null;
        break;
    }
}